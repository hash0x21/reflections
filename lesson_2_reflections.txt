git staging allows you to only add files in which you want to commit where there maybe alot of files that have been changed

adding a line to commit

How can you use the staging area to make sure you have one commit per logical change?

staging area allows you to specify exactly which files you want to commit so you can branch out your commits! 

What are some situations when branches would be helpful in keeping your history organized? How would branches help?

branches will help keep features or changes to your project separate and commits will only retain to that branch instead of being jumbled up into it master branch 

How do the diagrams help you visualize the branch structure?

Diagrams help understand where the branches are split into from the master branches and how the commit log history pertains to each branch ..

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?

When you merge two branches together, it holds all the latest code from both branches.. typcially you merge a feature branch into master. 

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?

If git can auto merge, that would be very easy and nice. manual is rquired when more inspection is required to determine how to merge conflicts 
